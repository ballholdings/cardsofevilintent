<?xml version="1.0" encoding="utf-8"?>

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:orientation="vertical">

    <TextView
        android:id="@+id/black_card"
        android:text="@string/black_card"
        android:layout_width="fill_parent"
        android:layout_height="200dp"
        android:textColor="#FF00FF"
        android:textSize="22sp"
        android:background="@drawable/black_card_bg"
        android:padding="15dp"/>

    <HorizontalScrollView
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:id="@+id/horizontalScrollView"
        android:layout_gravity="center_horizontal"
        android:layout_marginTop="@dimen/activity_vertical_margin">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="200dp"
        android:orientation="horizontal">

        <TextView
            android:id="@+id/white_card1"
            android:text="@string/white_card"
            android:layout_width="wrap_content"
            android:layout_height="fill_parent"
            android:textColor="#FF00FF"
            android:textSize="22sp"
            android:background="@drawable/tan_card_bg"
            android:padding="15dp"/>

        <TextView
            android:id="@+id/white_card2"
            android:text="@string/white_card"
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:textColor="#FF00FF"
            android:textSize="22sp"
            android:background="@drawable/tan_card_bg"
            android:padding="15dp"/>

        </LinearLayout>

    </HorizontalScrollView>

</LinearLayout>